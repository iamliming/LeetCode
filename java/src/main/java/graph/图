无向图
有向图
    有向无环图（DAG)

    有向有环图
    入度
       指向该节点的边
    出度
       从该节点出发的边
拓扑排序
	广度优先搜索
	   计算所有节点的入度。入度为0时候，代表已经搜索完
	深度优先搜索
		获取所有节点的出度。每个节点有三个状态：未搜索、搜索中、已搜索

图的数据结构
    顶点

    操作	    邻接列表	   邻接矩阵
    存储空间	O(V + E)	O(V^2)
    添加顶点	O(1)	    O(V^2)
    添加边	O(1)	    O(1)
    检查相邻性O(V)	    O(1)

https://www.jianshu.com/p/bce71b2bdbc8