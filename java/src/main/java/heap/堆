堆
堆是优先队列（Priority Queue）的实现，可以用于 动态选取 一个集合中的最值元素。堆通常指二叉堆，堆包括最大堆和最小堆：最大堆的每一个结点（除了根结点）的值不大于其父结点；最小堆的每一个结点（除了根结点）的值不小于其父结点
堆是完全二叉树
每个节点的值都必须大于等于或者小于等于其子节点

最大堆：每个节点都大于等于子节点
最小堆：每个节点都小于等于子节点

加入堆：如果堆未满，先插入到堆尾，再进行上移操作。堆已满比较堆顶元素和插入的元素大小，再进行下移操作
上移：
下移：
